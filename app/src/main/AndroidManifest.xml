<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bayu.sejarahbatang">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application

        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">

        <uses-library android:name="org.apache.http.legacy" android:required="false" />

        <activity android:name=".activities.MainActivity">

        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />


        <activity android:name=".activities.DataActivity">

        </activity>

        <activity android:name=".maps.MapKate">

        </activity>

        <activity android:name=".activities.DetailActivity">

        </activity>

        <activity android:name=".activities.KomentActivity">

        </activity>

        <activity android:name=".activities.DtActivity">

        </activity>

        <activity android:name=".activities.DtSejarah">

        </activity>

        <activity android:name=".activities.TambahKomen">

        </activity>

        <activity android:name=".activities.SplashscreenActivity"
            android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.TentangActivity">

        </activity>

        <activity
            android:name=".maps.MapActivity">

        </activity>

        <activity android:name=".mapskategori.MapKategoriActivity">

        </activity>

    </application>

</manifest>